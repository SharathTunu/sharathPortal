# AI / ML

## Model Training & Development

- Trained a **SigLIP model** to extract feature vectors from images for **Re-Identification (ReID)** of people across multiple cameras, achieving **80% accuracy**.
- Trained a **YOLOv11s model** to detect wooden pallets in warehouse environments with **98% accuracy**.
- Trained a **multi-class YOLOx-based person & bag detection model** with **90% accuracy**, used for abandoned baggage detection with Kalman filter-based tracking (~5% tracking loss).
- Trained a **YOLO-based car detection model** with **96% accuracy** for parking and overtime alert systems, with ~5% tracking loss.
- Trained a **YOLO-based person detection model** at **98% accuracy** for traffic flow heat map generation.
- Trained a **multi-class YOLOx color classification model** with **90% accuracy** for employee interaction monitoring using uniform color as a classifier.
- Trained a **YOLOv4-based detection model** with **Kalman filter tracking** to count foot and vehicle traffic at **95–98% accuracy**.
- Trained a **YOLOX-based model** to detect whether an aircraft engine is turned on at a gate, with **85% accuracy**.
- Trained a **multi class U-Net-based segementation model** to perform inference on vehicle telemtry with **95% accuracy** using cityscapes dataset.
- Trained a **multi class YOLACT++ segementation model** to perform inference on **below the wing** vehicles at an airport with **95% accuracy**.


## Algorithms & AI Systems

- Built a **Break Check algorithm** using YOLOv4, tracking, and Mean-Shift clustering to identify break compliance with up to **80% accuracy**.
- Developed a **Pose Estimation algorithm** using a skeleton model to count object "touches," achieving up to **85% accuracy**.
- Developed a **Checkout Summary algorithm** that computes average checkout times for customers at checkout stations with **98% accuracy**.
- Implemented a **ResNet-based classification model** to measure average employee–customer interaction times with **~95% accuracy**.
- Developed a **filtering algorithm** for loan offer generation based on user parameters and historical data.
- Implemented a **Neural Network-based Reinforcement Learning agent** (Deep Q-learning) that navigates autonomously in an unstructured 2D grid environment.
- Built an **Automatic Speech Recognition system** using RNN, DNN, and CNN architectures with TensorFlow and Caffe for commercial speech and NLP products.

## Vision-Language & LLM Integration

- Integrated **vLLM** into vision pipelines to automate QA of the ReID process at scale, reducing human review effort from **~50 man-hours to 10–15 hours**.
- Created a **RAG framework** based on my resume to create a chatbot, which can be used below on this portal.

## Frameworks & Tools

- Proficient in **PyTorch, TensorFlow, ONNX, TensorRT, DeepStream, OpenCV, YOLOv4, YOLOX, YOLOv11**.
- Strong skills in **Python and C++** for ML model development and deployment.
- Experience with **CNN, RNN, RCNN** architectures and deep learning at production scale.

